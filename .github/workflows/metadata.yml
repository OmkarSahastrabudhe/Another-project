name: metadata
on: 
  push:
    branches:
      - master
    paths: 
      - 'src/**' 
  workflow_dispatch:
    
jobs:
  metadata:
    runs-on: ubuntu-latest
    steps:
      - name: printing-metadata
        run: echo " ${{toJSON(github)}}"

